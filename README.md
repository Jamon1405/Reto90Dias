# Reto90Dias

Para iniciar el proyecto instala las dependencias y luego ejecuta los scripts de desarrollo o linting.

```bash
npm install
npm run dev
npm run lint
```

Esto evitará errores como `next: command not found` que aparecen si las dependencias no han sido instaladas.
